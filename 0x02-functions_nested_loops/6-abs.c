/**
  * _abs - get the absolute value of inger number
  * @n: intger number
  *
  * Return: The absolute value of input number
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-1 * n);
	}
}
